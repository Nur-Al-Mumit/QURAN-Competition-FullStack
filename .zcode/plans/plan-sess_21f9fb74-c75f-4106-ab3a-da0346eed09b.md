## Final Round ID Card Implementation Plan### OverviewCreate a new `/admin/final-round-id-card` page with portrait-oriented, duplex-printable ID cards (front + back). The card size will be **85.6mm × 54mm** (same as training-id-card), with a beautiful design.### Data Logic (Backend Controller)Create `FinalRoundIdCardController.php` with a `data()` endpoint that combines two queries:**1. Mahir (criteria_id = 1) — ALL seasons:**- Query `user_preliminary_results` where `criteria_id = 1`- No season filter, no final_confirmation check- Eager-load: `userCompetitionForm` (reg_no, name_en, name_bn, phone, dob, criteria_id), `userCompetitionForm.season` (season_name, season_year), `attendanceAllocation` (group, serial)**2. Mubtadi (criteria_id = 2) — CURRENT active season ONLY + must have final_confirmation:**- Query `user_preliminary_results` where `criteria_id = 2` AND `season_id = active_season_id`- JOIN / whereHas with `user_final_confirmations` to ensure a confirmation row exists- Eager-load same relations as above**Combine both results**, add a computed `criteria_label` field (1 → 'মাহির / Mahir', 2 → 'মুবতাদী / Mubtadi'), sort by serial, and return.Also add a `seasons()` endpoint returning all seasons + active season ID.### Backend RouteCreate `backend/routes/api/admin/final-round-id-card.php`:- `GET /admin/final-round-id-card/seasons` → `seasons()`- `GET /admin/final-round-id-card/data` → `data()`Register in `backend/routes/api/admin/api.php`.### Frontend PageCreate `frontend/pages/admin/(ADMIN)/final-round-id-card.vue` — a single-file Vue component modeled after `final-contest-id-card.vue` but with training-id-card sizing.**Card Size:** 85.6mm × 54mm (portrait, same as training-id-card)**Front Side Design:**- Decorative accent bar at top (gradient, matching theme)- Header: 'বিশুদ্ধ কুরআন প্রতিযোগিতা' + Hijri year '১৪৪৮ হিজরি'- Tier strip: 'ফাইনাল রাউন্ড পাস কার্ড' with criteria badge (মাহির / মুবতাদী)- Name block: Bengali name + English name- QR code (data-URL based, encoding reg_no) for registration verification- Footer row: Reg No, Group, Serial- Organization footer: logo + 'এসো কুরআন শিখি'**Back Side Design:**- Same accent bar at top for visual matching- Centered organization logo- Organization name 'এসো কুরআন শিখি'- Organization QR code (linking to eshoquranshikhi.org)- Decorative divider- Footer text**Duplex Printing (CSS):**- Screen: single-column centered preview with FRONT/BACK labels per card- Print: 2-column grid (`repeat(2, 85.6mm)`), `break-before: page` on backs section- Same global visibility hack as final-contest-id-card to hide layout chrome- Dashed border cutting guides in print mode- `@page { size: A4 portrait; margin: 0 }`**QR Generation:**- Use `qrcode` npm package's `toDataURL()` for base64 images (same approach as final-contest-id-card)- Generate per-student QR from `reg_no`- Generate one org-level QR for `https://eshoquranshikhi.org/`### Navigation & Auth- Add sidebar entry in `navigationLinks.ts` under appropriate group- Add `/admin/final-round-id-card` to `adminAuthRoutes.ts` protectedPaths### Files to Create/Modify1. **CREATE** `backend/app/Http/Controllers/Api/V1/Admin/FinalRoundIdCardController.php`2. **CREATE** `backend/routes/api/admin/final-round-id-card.php`3. **MODIFY** `backend/routes/api/admin/api.php` — include new route file4. **CREATE** `frontend/pages/admin/(ADMIN)/final-round-id-card.vue`5. **MODIFY** `frontend/composables/navigationLinks.ts` — add sidebar link6. **MODIFY** `frontend/utils/adminAuthRoutes.ts` — add protected path